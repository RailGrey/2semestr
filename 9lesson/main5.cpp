#include <iostream>

struct denom_array_t {
    unsigned * arr;
    unsigned sz;
};

struct denom_array_t egyptian_fractions (unsigned num, unsigned den) {

}

int main() {
    int num, den;
    std::cin >> num >> den;
    egyptian_fractions(num, den);
    return 0;
}

